engine_catalog:
  version: v1.1
  status: CANON_PUBLIC_SAFE
  statuses: [ACTIVE, STANDBY, DISABLED, REMOVED]
  engines:
    - {id: engine.codex, status: ACTIVE, lane: L1, roles: [code_impl, patch, refactor]}
    - {id: engine.claude_code, status: STANDBY, lane: L1, roles: [review, infra, incident_fix]}
    - {id: engine.openai_gpt, status: STANDBY, lane: L1, roles: [general, reasoning, thinking_optional]}
    - {id: engine.anthropic_claude, status: STANDBY, lane: L1, roles: [writing, review, thinking_optional]}
    - {id: engine.gemini, status: STANDBY, lane: L2, roles: [multimodal]}
    - {id: engine.kimi_2_5, status: STANDBY, lane: L2, roles: [alt_worker]}
    - {id: engine.minimax_m2, status: STANDBY, lane: L2, roles: [alt_worker]}
    - {id: engine.nanucai_cllm_cf, status: STANDBY, lane: L1, roles: [cllm_internal, elastic_socorro]}
    - {id: engine.nanucai_cllm_hz, status: STANDBY, lane: L1, roles: [cllm_internal, gpu_dedicada]}
