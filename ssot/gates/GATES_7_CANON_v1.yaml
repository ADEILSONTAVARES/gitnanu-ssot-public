version: "v1.0"
gates: "GATES_7_CANON"
statement: "Ordem canônica dos 7 gates BASELINE antes de publicar qualquer mudança persistente."

ordem:
  G1: "Firewall policy check (ALLOW/DENY) + reasons"
  G2: "Secrets/PII scan (heurístico + patterns)"
  G3: "Schema validation (JSON Schema / YAML sanity)"
  G4: "No Holes (registries + required exposures)"
  G5: "Doctor1 (preflight) PASS"
  G6: "Doctor2 (audit completo) PASS"
  G7: "Evidence Pack (receipt + hashes + refs) PASS"

invariants:
  - "Early-exit no primeiro FAIL"
  - "Sem Evidence Pack => FAIL para publish"
  - "Sem tags alinhadas (latest + patchpack) => FAIL para release"
