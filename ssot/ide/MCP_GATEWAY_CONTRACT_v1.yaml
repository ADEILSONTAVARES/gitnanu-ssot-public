ssot_id: MCP_GATEWAY_CONTRACT_v1
version: v1
status: CANONICAL
created_at: "2026-03-09"

meta:
  title: "MCP Gateway Contract — IDE <-> MCP layer"
  sistema: NANUCAI OG10

contrato:
  entrada:
    job_id: "JOB_{uuid}"
    studio_id: "S00-S51"
    mcp_id: "identificador do MCP"
    payload: "dados da chamada"
    evidence_ref: "opcional"

  saida:
    resultado: "dados retornados"
    latencia_ms: "int"
    custo_usd: "float"
    evidence_ref: "se gerou evidence"
    status: "PASS | FAIL | QUARANTINE"

  garantias:
    - "MCP fora da allowlist = BLOCK automatico"
    - "Resultado sempre retorna evidence_ref se fez claim"
    - "Timeout default: 30s (configuravel por MCP)"
    - "Retry: ate 3x via Ralph_Loop antes de FAIL"
    - "Cache: lookup antes de chamada (Cache_Quantum_Lookup)"

  firewall:
    pointer: "ssot/protocols/UNIVERSAL_PROTOCOLS_v1.yaml#mcp_firewall"
    allowlist_pointer: "ssot/mcp/MCP_ELITE_25_CANON.yaml"
