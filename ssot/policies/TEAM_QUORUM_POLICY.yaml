version: "v1.0"
policy: TEAM_QUORUM_POLICY

default_quorum:
  simple_change: "1 reviewer"
  medium_change: "2 reviewers"
  critical_change: "2 reviewers + Doctor2"

tie_break:
  - "PrM decides based on acceptance_criteria"
  - "If confidence < 0.70 -> require Doctor2"
  - "Council only if still ambiguous after Doctor2"

cost_safety:
  - "Avoid council by default"
  - "Council allowed only when: risk=high AND ambiguity persists"
