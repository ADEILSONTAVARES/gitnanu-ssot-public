universal_protocols:
  version: "v1.0"
  ucp:
    statement: "Universal Context Protocol (handle/pointer, nunca raw)"
    schema: {context_id: "CTX_*", uri: "gndrive://...", metadata: {}}
  ap2:
    statement: "ActionPack Protocol v2 (idempotência + budgets)"
    schema: {action_id: "A*_xxx", idempotency_key: "sha256", timeout_ms: 30000}
  a2a:
    statement: "Agent-to-Agent request"
    schema: {from_agent: "S*", to_agent: "S*", request_type: "string", payload: {}}
  firewall:
    etapas: [policy_check, dlp_scan, guardrails_scan, rate_limit_check, budget_check]
