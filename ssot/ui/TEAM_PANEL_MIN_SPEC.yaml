version: "v1.0"
ui_spec: TEAM_PANEL_MIN_SPEC
location: "IDE_2x1_inside_GammaNotion"

panels:
  left:
    - "Team Session Overview"
    - "Active Locks"
    - "PAC List"
  right:
    - "Definition of Done (live)"
    - "Pending Reviews (diff preview)"
    - "Evidence Pack Status"
    - "Kill Switch Status"

requirements:
  - "No direct write: only propose patches"
  - "Apply patch only via GitNanu pipeline"
